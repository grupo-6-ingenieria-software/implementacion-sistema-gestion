import { controllers } from '../../shared/controllers';
import { createNotImplementedController } from './base';

export const accessControlController = createNotImplementedController(
  controllers[2],
);
